registry {
  # 配置格式：file 、nacos 、eureka、redis、zk
  type = "eureka"

  # eureka 服务注册中心
  eureka {
    serviceUrl = "http://localhost:8761/eureka"
    application = "default"
    weight = "1"
  }
}

config {
  # 配置格式：file、nacos 、apollo、zk
  type = "file"

  file {
    name = "file.conf"
  }
}
